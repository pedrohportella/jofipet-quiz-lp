# Story 2.1: Hero + entrada do quiz

## Status

Draft

## Story

**As a** tutor visitando a LP vinda de anúncio Meta,
**I want** entender em 3 segundos o que a página oferece e iniciar o quiz com um toque,
**so that** decido continuar ou abandonar sem fricção.

## Acceptance Criteria

1. Hero renderiza headline, subheadline, selo de social proof ("+X tutores já descobriram"), e CTA primário
2. CTA "Descobrir meu plano ideal" inicia o quiz (transição suave para a primeira pergunta)
3. Hero carrega em <1s em 4G (critical CSS inline, imagens otimizadas com `next/image`)
4. Copy do hero extraído de `config/content.json` (editável sem deploy de código)
5. UTMs são preservadas no state global do quiz ao iniciar
6. Mobile Safari e Chrome Android renderizam sem shift de layout (CLS <0.05 na fold)

## Tasks / Subtasks

- [ ] Config de conteúdo (AC: 4)
  - [ ] Criar `config/content.json` com seção `hero` (architecture §11.4)
  - [ ] Loader em `src/lib/content.ts` (tipado com Zod)
- [ ] Componente Hero (AC: 1, 3, 6)
  - [ ] `src/components/hero/Hero.tsx` — Server Component (não precisa client-side)
  - [ ] Usar `next/image` para ilustração com `priority` + blur placeholder
  - [ ] Critical CSS via Tailwind inline (sem lazy classes)
  - [ ] Layout mobile-first conforme wireframe front-end-spec §4.1 Screen 1
  - [ ] Selo social proof com contador placeholder (valor estático MVP via config)
- [ ] Integração com quiz (AC: 2, 5)
  - [ ] CTA é um Server Action ou client-component que:
    - Dispara `useUtms()` para persistir UTMs da URL
    - Dispara `useQuizState().dispatch({ type: 'START' })`
    - Navega via `router.push('/quiz/0')`
  - [ ] Transição cross-fade 320ms entre Hero e Quiz (ver front-end-spec §9.2)
- [ ] Performance (AC: 3, 6)
  - [ ] Fontes via `next/font/google` (Nunito) com preload
  - [ ] Hero SVG/AVIF ≤40KB, width+height explícitos (previne CLS)
  - [ ] Medir com Lighthouse local; LCP <1s
- [ ] Tracking (AC: 5)
  - [ ] `trackPageView()` é automático (Story 1.4)
  - [ ] `gtag('event', 'quiz_start', { variant, utm_source })` no click do CTA

## Dev Notes

**Source artifacts:**
- Architecture §3 (structure `src/components/hero/`), §8 (tokens), §11.4 (content.json), §16 rule 9 (client components)
- Front-end Spec §4.1 Screen 1 (wireframe completo), §9.2 (animations), §10 (performance strategies)
- PRD FR1

**Dependency:** Stories 1.1, 1.2, 1.4 merged.

**Critical rules:**
- Hero pode ser Server Component (sem estado interativo próprio); apenas o botão CTA é client (`'use client'`)
- UTMs devem ser capturadas via client hook — não no Server Component
- `next/image` obrigatório para todas as imagens (NFR optimization)

**Copy placeholders:** Usar copy do `config/content.json` exemplo em architecture §11.4. Paulo Portella (PP) pode iterar editando apenas o JSON depois.

### Testing

Referência: architecture §9.1. Obrigatório:
- `Hero.test.tsx` — render test + clique no CTA dispara navegação (mockar `useRouter`)
- Manual: Lighthouse em preview Vercel confirma LCP <1s mobile + CLS <0.05
- Visual regression via screenshots Playwright (pós-MVP)

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-04-17 | 0.1 | Initial draft sharded from PRD | @po (aiox-master) |
