# Story 2.3: API route proxy RD Station Marketing

## Status

Ready for Review — API route completa + RD Station client/mapper + Turnstile + rate-limit + 11 mapper tests passing

### Execution Notes (2026-05-12)

- ✅ AC1: `POST /api/leads` valida payload via `LeadPayloadSchema` (Zod) em [route.ts:74](../../src/app/api/leads/route.ts:74)
- ✅ AC2: Turnstile server-side em [turnstile/verify.ts](../../src/lib/turnstile/verify.ts) — gracioso skip quando `TURNSTILE_SECRET_KEY` ausente (MVP)
- ✅ AC3: Rate limit 10 req/IP/10min implementado em [rate-limit/in-memory.ts](../../src/lib/rate-limit/in-memory.ts) — tech debt: migrar pra Vercel KV em Story 2.4
- ✅ AC4: `buildRdConversionPayload` em [mapper.ts](../../src/lib/rd-station/mapper.ts) gera envelope `event_type: CONVERSION` + `event_family: CDP` + custom fields `cf_quiz_tier`, `cf_quiz_score`, `cf_pet_*`, `cf_utm_*` + tags `lead-{tier}` e `quiz-jofipet`
- ✅ AC5: Token RD via `process.env.RD_STATION_PUBLIC_TOKEN` server-only (nunca exposto) em [route.ts:103](../../src/app/api/leads/route.ts:103); skip gracioso quando ausente (lead aceito + warning)
- ✅ AC6: Response sempre com `{success, correlationId, leadId?, reason?, warning?}` — 200/400/429
- ✅ AC7: Logs estruturados JSON com `correlationId` em todos eventos (rate_limit_hit, payload_invalid, turnstile_failed, rd_conversion_*)
- ⚠️ AC8: MSW handlers ainda não criados — mapper tests cobrem a lógica de transformação; integration test com MSW = tech debt @qa

**Decisões técnicas:**
- `runtime = 'nodejs'` (não 'edge' — facilita debug local + suporta KV migration sem refactor)
- Email placeholder quando ausente: `{slug-do-nome}-{whatsapp-digits}@no-email.jofipet.local` — RD aceita, evita conflito de contato sem email
- Email real preferido quando fornecido pelo lead
- **4xx do RD = bug nosso**: response 200 com `warning: 'rd_rejected'` (UX não quebra; log error pra investigação)
- **5xx/timeout do RD = queue**: response 200 com `warning: 'rd_5xx_queued'` ou `'rd_unreachable_queued'` (Story 2.4 implementa queue real — por enquanto warning)
- **Sem RD token configurado**: response 200 com `warning: 'rd_token_missing'` (MVP early — só captura)

**Adições nesta passada:**
- ✅ Novo `src/lib/rd-station/client.ts` — postConversion com timeout 5s via AbortController
- ✅ Novo `src/lib/rd-station/mapper.ts` — buildRdConversionPayload + ANSWER_FIELD_MAP canonical
- ✅ Novo `src/lib/rd-station/mapper.test.ts` — 11 testes (CONVERSION envelope, conversion_identifier por tier, E.164, tags, custom fields, UTMs, array answers, unknown keys)
- ✅ Novo `src/lib/turnstile/verify.ts` — POST siteverify Cloudflare com timeout 3s
- ✅ Novo `src/lib/rate-limit/in-memory.ts` — Map-based bucket + WINDOW_MS/MAX_HITS
- ✅ Novo `src/app/api/leads/route.ts` — handler completo POST

**Comandos de validação rodados:**
- `npm run typecheck` ✅
- `npx vitest run src/lib/rd-station/ src/lib/validation/` → 25/25 passing ✅
- `npx vitest run` (suite completa) → 104/104 ✅

**Tech debt registrado:**
- ⚠️ MSW integration test (`route.test.ts`) — adicionar antes do merge final
- ⚠️ Vercel KV substitui rate-limit in-memory (atualmente in-memory não persiste entre invocações de serverless)
- ⚠️ Real-world test: enviar um lead em staging quando `RD_STATION_PUBLIC_TOKEN` for setado no Vercel, validar que aparece no painel RD com cf_* corretos — `docs/qa/rd-staging-test.md` quando rodar

## Story

**As a** Pedro (decisor),
**I want** que cada lead capturado apareça no RD Station com tag do tier e todas as respostas do quiz como campos customizados,
**so that** consigo segmentar automações e usar os dados em lookalikes Meta.

## Acceptance Criteria

1. Endpoint `POST /api/leads` recebe payload `{ name, whatsapp, email?, consent, tier, answers, utms, turnstile_token }`
2. Valida Turnstile token server-side; rejeita se inválido
3. Rate limit 10 req/IP/10min usando Vercel KV como counter
4. Chama RD Station Marketing `/platform/conversions` com: `event_type: CONVERSION`, `event_family: CDP`, `payload: { conversion_identifier: 'jofipet-quiz-{tier}', email, name, mobile_phone, tags: [`lead-{tier}`, `quiz-jofipet`], custom_fields: {...answers, ...utms, score_breakdown } }`
5. Token RD Station lido de env var server-side `RD_STATION_TOKEN` — nunca exposto
6. Retorna `{ success: true, leadId, correlationId }` em 200 ou `{ success: false, reason, correlationId }` em 4xx/5xx
7. Logs estruturados em Vercel (JSON) com correlationId para rastreamento
8. Mocks MSW cobrem happy path + falha 500 em testes de integração

## Tasks / Subtasks

- [ ] Resolver open questions #1 #2 antes do merge
  - [ ] Pedro confirma: API token simples (Bearer) ou OAuth2 client_credentials
  - [ ] Staging test: RD aceita custom fields `cf_*` sem pre-configuração ou precisa criá-los antes
- [ ] RD Station client (AC: 4, 5)
  - [ ] `src/lib/rd-station/client.ts` — template completo em architecture §6.1
  - [ ] Timeout 5s via AbortController
  - [ ] `RdStationError` class com status + correlationId
  - [ ] Token em env var `RD_STATION_TOKEN` server-only
- [ ] Payload mapper (AC: 4)
  - [ ] `src/lib/rd-station/mapper.ts` — template em architecture §6.4
  - [ ] `normalizePhoneBR` converte `(11) 91234-5678` → `+5511912345678`
  - [ ] Placeholder email quando não fornecido: `lead-{uuid}@no-email.jofipet.local` (confirmar em staging)
- [ ] API route handler (AC: 1, 2, 3, 6, 7)
  - [ ] `src/app/api/leads/route.ts` — template completo em architecture §6.2
  - [ ] `runtime = 'edge'`
  - [ ] Extração IP via `x-forwarded-for` ou `x-real-ip`
  - [ ] Parse + validação com `LeadPayloadSchema`
  - [ ] Logs estruturados JSON com `correlationId`
- [ ] Rate limit (AC: 3)
  - [ ] `src/lib/kv/rate-limit.ts` com Vercel KV INCR + EXPIRE
  - [ ] Chave `rate:leads:{ip}`, max 10, window 600s
  - [ ] 429 com mensagem "rate_limited"
- [ ] Turnstile (AC: 2)
  - [ ] `src/lib/turnstile/verify.ts` POST para `https://challenges.cloudflare.com/turnstile/v0/siteverify`
  - [ ] Secret em env var `TURNSTILE_SECRET_KEY`
  - [ ] Inclui IP no payload de verificação
- [ ] Lead store (AC: 4)
  - [ ] `src/lib/kv/leads-store.ts` com `storeLead` — chave `lead:{leadId}` TTL 30 dias
  - [ ] Grava metadata mesmo se RD falhar (feed do admin)
- [ ] Tests (AC: 8)
  - [ ] MSW handlers em `src/test/msw/handlers.ts` para RD success + RD 500 + Turnstile success + fail
  - [ ] Integration test em `src/app/api/leads/route.test.ts` (Vitest + MSW)

## Dev Notes

**Source artifacts:**
- Architecture §6 (API integration — **referência primária**), §10 (env vars), §14.1 (defenses), §16 (critical rules 2, 5, 6)
- PRD FR11, FR12, NFR6, NFR7, NFR8

**Dependency:** Story 1.3 (scoring existe para gerar tier no client); Story 2.2 (captura gera payload). Stories 2.1, 2.2 podem estar em paralelo com mock.

**Critical rules:**
- Token RD **nunca** exposto — lint custom bloqueia acesso fora de `src/app/api/` e `src/lib/rd-station/` (rule 2)
- Toda fetch com timeout (rule 6) — 5s
- Response sempre `{ success, correlationId, ...rest }` (rule 5)
- Correlation ID retornado ao client para rastreio

**4xx vs 5xx behavior:**
- RD Station **5xx ou timeout** → queue (Story 2.4)
- RD Station **4xx** (payload inválido) → log error + retornar `success: true, warning: 'rd_rejected'` (UX não quebra; bug é nosso)
- Turnstile fail → 400 imediato
- Rate limit hit → 429 imediato

### Testing

Referência: architecture §9.1, §6. Obrigatório:
- Integration test com MSW cobre: happy path, RD 500, RD 400, Turnstile fail, rate limit
- Coverage 95% em `src/lib/rd-station/*`, 90% em route handler
- Manual: test event real em RD Station staging (se disponível) — screenshot em `docs/qa/rd-staging-test.md`

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-04-17 | 0.1 | Initial draft sharded from PRD | @po (aiox-master) |
