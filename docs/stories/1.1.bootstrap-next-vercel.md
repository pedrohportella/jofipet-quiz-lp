# Story 1.1: Bootstrap Next.js + Vercel + canary

## Status

InProgress — código local completo + canary validado em preview; aguarda handoff @devops para repo/vercel/DNS

### Execution Notes (2026-04-17)

- ✅ Next.js 14 + TS strict + Tailwind + ESLint + jsx-a11y + Prettier scaffoldados manualmente (sem create-next-app)
- ✅ Canary `/` renderiza mobile-first com timestamp de build; snapshot confirma headline + subcopy + build timestamp
- ✅ `npm install` completa em 57s (949 packages)
- ✅ `src/app/layout.tsx` com Nunito (latin-ext) via `next/font/google`, viewport, metadata
- ✅ `src/app/globals.css` com tokens CSS + `prefers-reduced-motion`
- ✅ CI workflow (`.github/workflows/ci.yml`) e Lighthouse workflow (`.github/workflows/lighthouse.yml`) criados — aguardam repo no GitHub para disparar
- ✅ `lighthouserc.json` com barras perf 85 / a11y 95 / best-practices 90 / seo 90
- ✅ `vitest.config.ts` com coverage thresholds 70%
- ⚠️ **Tech debt registrado:** `next@14.2.3` tem security advisory (dezembro 2025). Upgrade para versão patched antes de ship produção (spawned task separada). Canary em dev fica ok.
- ⚠️ **Tech debt registrado:** `@vercel/kv` deprecado — migrar para Upstash Redis no Epic 2. Story 2.4 observa.
- ⏭️ **Handoff @devops pendente:** `gh repo create`, `vercel link`, DNS `quiz.jofipet.com.br`, GitHub branch protection, secrets setup
- ⏭️ **Handoff @devops:** conectar Vercel KV store (retornado via Upstash)

## Story

**As a** dev,
**I want** inicializar o projeto Next.js 14 com TypeScript e deployar em Vercel com uma canary page,
**so that** há base funcional com CI/CD automático para acelerar iterações.

## Acceptance Criteria

1. `projects/jofipet-quiz-lp/` contém projeto Next.js 14 App Router + TypeScript strict + Tailwind + ESLint + Prettier
2. `/` renderiza canary page com headline "Jofi Pet Quiz — em construção" e timestamp de build
3. Deploy Vercel funciona via git push; preview deployment por branch habilitado
4. Domínio custom (`quiz.jofipet.com.br` ou placeholder Vercel) acessível com HTTPS
5. Lighthouse CI configurado em `.github/workflows/` com barras: a11y ≥95, perf mobile ≥85
6. README do projeto documenta setup local e scripts disponíveis (`dev`, `build`, `lint`, `test`)

## Tasks / Subtasks

- [ ] Bootstrap Next.js (AC: 1)
  - [ ] Rodar `npx create-next-app@latest` com flags: `--typescript --tailwind --app --src-dir --import-alias "@/*" --eslint`
  - [ ] Adicionar `.prettierrc` + `eslint-plugin-jsx-a11y` em `.eslintrc.json`
  - [ ] Configurar `tsconfig.json` com `strict: true` e paths `@/*`
  - [ ] Adicionar `tailwind.config.ts` com tokens Jofi (architecture.md §8.2)
- [ ] Canary page (AC: 2)
  - [ ] `src/app/page.tsx` com headline + timestamp via `new Date().toISOString()` em build time
  - [ ] Aplicar layout básico mobile-first com container max-w-mobile
- [ ] Setup Git + Vercel (AC: 3, 4)
  - [ ] **[delegar @devops]** `gh repo create jofipet-quiz-lp` (private) e primeira commit
  - [ ] **[delegar @devops]** `vercel link` + `vercel --prod`
  - [x] ~~Configurar DNS CNAME~~ — **DECISÃO 2026-05-12 (Pedro):** usar placeholder Vercel `jofipet-quiz.vercel.app` para go-live inicial. Migração para domínio próprio (`quiz.jofi.pet` recomendado) fica para tech debt depois que o time tiver acesso ao DNS do .pet — antes de qualquer campanha paga, pois `.vercel.app` em ad reduz CTR.
- [ ] Lighthouse CI (AC: 5)
  - [ ] Criar `.github/workflows/lighthouse.yml` (ver architecture.md §15 deploy flow)
  - [ ] Criar `lighthouserc.json` com barras: perf 85, a11y 95, best-practices 90, seo 90
  - [ ] Marcar como required status check no branch main
- [ ] README (AC: 6)
  - [ ] Atualizar README.md do projeto com seção "Setup" (clone, install, dev) e "Scripts"

## Dev Notes

**Source artifacts:**
- Architecture §1 (starter template), §3 (project structure), §8 (Tailwind config), §15 (deploy strategy), §17 (quick reference)
- PRD §4 (Technical Assumptions)

**Relevant source tree:**
```
projects/jofipet-quiz-lp/
├── next.config.mjs         # security headers (archi §14.1)
├── tailwind.config.ts      # tokens Jofi (archi §8.2)
├── tsconfig.json
├── lighthouserc.json       # barras LH
├── .github/workflows/
│   ├── ci.yml
│   └── lighthouse.yml
└── src/app/
    ├── layout.tsx
    ├── page.tsx            # canary
    └── globals.css
```

**Delegation:** Todas tarefas de `git push`, `gh repo`, `vercel` são **exclusivas @devops** (ver `.claude/rules/agent-authority.md`). Dev prepara código e commits locais; @devops toma a partir daí.

**Open questions não bloqueantes:**
- Domínio final (#6) — pode deployar em placeholder Vercel inicialmente
- Paleta confirmada por DM (#7) — tokens provisórios do architecture §8.2 suficientes para canary

### Testing

Referência: architecture.md §9. Nesta story não há testes funcionais a rodar (canary), mas:
- Vitest deve estar configurado em `vitest.config.ts` com setup vazio
- Comando `pnpm test` deve rodar sem erro
- ESLint + typecheck passam em `pnpm lint` e `pnpm typecheck`

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-04-17 | 0.1 | Initial draft sharded from PRD | @po (aiox-master) |
