# Story 3.1: Tela de resultado — estrutura + diagnóstico dinâmico

## Status

Ready for Review — componentes por tier + result-template + 11 testes; aguarda @qa gate

### Execution Notes (2026-05-12)

- ✅ AC1: `/resultado/[tier]/page.tsx` valida enum + `ResultClient` lê tier/answers/leadName via `useQuizState()` — redirect `/` se state vazio
- ✅ AC2: Templates inline em [result-template.ts](../../src/lib/quiz/result-template.ts) com placeholders `{primeiroNome}`, `{especie}`, `{idade}`, `{preocupacao}`, `{gastoMensal}`, `{planoAtual}`
- ✅ AC3: Headline + subheadline + 3 bullets de diagnóstico por tier — implementados em `getHeadline`/`getSubheadline`/`getBullets`
- ✅ AC4: Emoji por tier — 🔥 quente, 🌻 morno, 💙 frio + kicker copy contextual
- ✅ AC5: Fallback gracioso para placeholders ausentes ("do seu pet", "o cuidado com o pet", "esse valor")
- ✅ AC6: Tracking `InitiateCheckout` em quente/morno via `useEffect`; `ViewContent` (`fbqTrack('ViewContent', {content_name: 'result_cold'})`) em frio

**Adições nesta passada:**
- `src/lib/quiz/result-template.ts` + `.test.ts` (11 testes: var extraction, fallbacks, render, bullets sem placeholder cru)
- `src/components/result/ResultBullets.tsx` (stagger Framer Motion 80ms entre items)
- `src/components/result/Result{Hot,Warm,Cold}.tsx`
- Split server/client: `page.tsx` é Server Component (static params + envs públicas) + `result-client.tsx` é Client (useQuizState + redirects)
- QuizContext: novo action `CAPTURE_LEAD` persiste `leadName` + `leadId` (Result lê daí)

**Decisões pragmáticas:**
- Templates inline (não config/results.json) — MVP. Migrar pra JSON quando Pedro quiser editar copy sem deploy.
- Headlines fixas por tier (não AB-testáveis ainda)

## Story

**As a** tutor que completou o quiz,
**I want** ver um diagnóstico que reflita o que respondi e inclua o nome do meu pet,
**so that** sinto que foi feito para mim e não é mais um resultado genérico.

## Acceptance Criteria

1. Rota `/resultado` lê tier + answers + leadId do state (ou query params fallback)
2. Template por tier em `config/results.json` com placeholders `{pet_name}`, `{pet_species}`, `{top_concern}`, `{monthly_spend}`
3. Render inclui headline customizado + 3 bullets de diagnóstico + CTA do tier
4. Se tier = `quente` → ilustração 🔥 + copy de urgência; `morno` → ilustração 🟡 + copy de economia; `frio` → ilustração 🔵 + copy educativa
5. Fallback gracioso se respostas específicas não foram dadas (template sem placeholder quebrado)
6. Evento Meta Pixel `InitiateCheckout` dispara ao renderizar resultado com tier quente ou morno; `ViewContent` para frio

## Tasks / Subtasks

- [ ] Config results (AC: 2)
  - [ ] `config/results.json` conforme schema architecture §11.3
  - [ ] Loader tipado em `src/lib/content.ts` (reusa infra da Story 2.1)
- [ ] Template engine (AC: 2, 5)
  - [ ] `src/lib/quiz/result-template.ts` com `renderTemplate(str, vars)` puro
  - [ ] Suporta placeholders `{nome}`, `{especie_label}`, etc.
  - [ ] Fallback: placeholder não encontrado → string vazia (não quebra layout)
- [ ] Componentes por tier (AC: 3, 4)
  - [ ] `src/components/result/ResultHot.tsx` (🔥 + WhatsApp CTA)
  - [ ] `src/components/result/ResultWarm.tsx` (🟡 + Sereninho CTA)
  - [ ] `src/components/result/ResultCold.tsx` (🔵 + artigos)
  - [ ] `src/components/result/ResultBullets.tsx` com stagger animation (80ms entre bullets)
- [ ] Rota dinâmica (AC: 1)
  - [ ] `src/app/resultado/[tier]/page.tsx` — param `tier` valida enum `quente|morno|frio`
  - [ ] Lê state via `useQuizState()` para obter answers + leadId
  - [ ] Fallback: se state vazio (reload direto), redireciona para `/`
- [ ] Tracking (AC: 6)
  - [ ] `trackEvent('InitiateCheckout', { content_name: tier, value: tier === 'morno' ? 49.90 : 0, currency: 'BRL' })` para quente/morno
  - [ ] `trackEvent('ViewContent', { content_name: 'result_cold' })` para frio

## Dev Notes

**Source artifacts:**
- Architecture §3 (structure `src/components/result/`), §11.3 (results.json), §13.1 (Meta events)
- Front-end Spec §4.1 Screens 4, 5, 6 (wireframes), §9.2 (stagger animation)
- PRD FR14, FR15

**Dependency:** Stories 1.2 (state), 1.3 (tier calc), 1.4 (tracking). Stories 3.2, 3.3, 3.4 pegam essa base e adicionam o CTA específico.

**Critical rules:**
- Template rendering puro (rule 14)
- Componentes `'use client'` (animações + hooks) — rule 9
- Ilustrações SVG inline ≤15KB (front-end spec §6.4)

**Personalization:** Valores dos placeholders vêm de:
- `{nome}` → lead.name (do QuizContext post-captura)
- `{especie_label}` → labels mapeadas de `config/quiz.json` (especie=cao → "cão")
- `{preocupacao_label}` → idem
- `{idade_label}` → idem

Map `answerId → label` deve ser extraído do `config/quiz.json` no loader.

### Testing

- Unit: `result-template.test.ts` cobre substituição + fallback de placeholder
- Integration: `/resultado/quente` renderiza com fixture de state; eventos Meta mockados são chamados
- Visual: screenshots Playwright (pós-MVP)

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-04-17 | 0.1 | Initial draft sharded from PRD | @po (aiox-master) |
