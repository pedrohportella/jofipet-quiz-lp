# Story 1.2: Quiz engine configurável (JSON-driven)

## Status

Ready for Review — implementação completa + Zod validation + 25 tests passing; aguarda @qa gate

### Execution Notes (2026-05-12)

- ✅ AC1: `<Quiz>` lê `config/quiz.json` via [loader.ts:30](../../src/lib/quiz/loader.ts:30); 8 perguntas configuradas
- ✅ AC2: 4 tipos suportados — switch em [QuizStep.tsx:140-170](../../src/components/quiz/QuizStep.tsx:140)
- ✅ AC3: ProgressBar "N de M" em [QuizStep.tsx:110-114](../../src/components/quiz/QuizStep.tsx:110)
- ✅ AC4: BACK preserva respostas — reducer em [QuizContext.tsx:67-68](../../src/context/QuizContext.tsx:67)
- ✅ AC5: Single-choice auto-avança 200ms — `AUTO_ADVANCE_DELAY_MS` em [QuizStep.tsx:21,70](../../src/components/quiz/QuizStep.tsx:21)
- ✅ AC6: `pet-ativo=nao` → `/obrigado-sem-pet` redirect em [QuizStep.tsx:61-64](../../src/components/quiz/QuizStep.tsx:61)
- ✅ AC7: Framer Motion fade+slide 280ms ease-out em [QuizStep.tsx:117-122](../../src/components/quiz/QuizStep.tsx:117)
- ✅ AC8: sessionStorage `jofi-quiz-state-v1` com hydration gate em [QuizContext.tsx:23,168](../../src/context/QuizContext.tsx:23)

**Adições nesta passada:**
- ✅ Novo `src/lib/quiz/schema.ts` — Zod discriminated unions para Quiz/Scoring/CepCoverage
- ✅ `loader.ts` agora valida via `safeParse` no boot (build falha se config inválido)
- ✅ Novo `QuizContext.test.tsx` — 11 testes cobrindo START/ANSWER/NEXT/BACK/GO_TO/FINISH/RESET/HYDRATE + sessionStorage persistence + version mismatch fallback
- ✅ Novo `loader.test.ts` — 14 testes cobrindo getQuestionById, getAnswerLabel + 5 cenários de rejeição de schema malformado
- ✅ Fix lateral: `vitest.config.ts` (vite v5/v7 dedupe via cast) + `ga4.ts` (`dataLayer` tipo Record<string,unknown>[]) + instalação `@typescript-eslint/eslint-plugin@7`

**Coverage final (escopo Story 1.2):**
- `context/QuizContext.tsx`: 97.34% lines / 87% branches / 100% functions
- `lib/quiz/schema.ts`: 100% / 100% / 100%
- `lib/quiz/loader.ts`: 93.33% lines / 92% branches
- `components/quiz/QuizOption.tsx`: 98.43% lines (já existente da implementação anterior)

**Tech debt registrado (não bloqueia 1.2):**
- ⚠️ Cobertura de testes < 80% em `src/components/quiz/` por falta de testes em QuizStep, QuizMultiChoice, QuizScaleInput, QuizTextInput, QuizProgressBar, QuizBackButton. Sugestão @qa: avaliar se ficam para 4.4 (Lighthouse CI gate) ou bloqueiam merge.
- ⚠️ Threshold global de coverage 70% falha porque tracking (Story 1.4) + motion lib + hooks UTM ainda não cobertos.
- ⚠️ `vitest.config.ts` carrega vite duplicado (v5 nested em vitest + v7 raiz via @vitejs/plugin-react). Cast como `any` resolve, mas a longo prazo adicionar `overrides.vite: ^5.4.0` em package.json.

**Comandos de validação rodados:**
- `npm run typecheck` ✅
- `npm run lint` ✅ (1 warning não-bloqueante em MetaPixelScript — Story 1.4)
- `npx vitest run src/context/QuizContext.test.tsx src/lib/quiz/loader.test.ts` → 25/25 passing

## Story

**As a** tutor de pet,
**I want** responder perguntas encadeadas com progress visível e poder voltar,
**so that** consigo completar o quiz sem frustração ou medo de perder respostas.

## Acceptance Criteria

1. Componente `<Quiz>` lê `config/quiz.json` e renderiza dinamicamente 6–8 perguntas
2. Suporta tipos: `single-choice`, `multi-choice`, `scale`, `text-input`
3. Progress bar fixa no topo mostra "pergunta N de M"
4. Botão "Voltar" preserva respostas anteriores dentro da sessão (state em React)
5. Single-choice auto-avança 200ms após seleção; demais tipos exigem botão "Próxima"
6. Primeira pergunta `pet-ativo` é eliminatória: resposta "Não" redireciona para rota `/obrigado-sem-pet`
7. Transições entre perguntas usam Framer Motion (fade + slide, 250ms)
8. State do quiz persiste em `sessionStorage` para resistir a reload acidental (não em localStorage — evitar contaminação cross-sessão)

## Tasks / Subtasks

- [x] Criar config + loader (AC: 1)
  - [x] Criar `config/quiz.json` com schema exato de architecture §11.1
  - [x] Criar `src/lib/quiz/loader.ts` que importa e valida via Zod
  - [x] Criar `src/lib/quiz/types.ts` com `QuizConfig`, `Question`, `Answer`, `Tier`
- [x] QuizContext + reducer (AC: 4, 8)
  - [x] Implementar `src/context/QuizContext.tsx` conforme architecture §5.2
  - [x] Persistência via sessionStorage com `SESSION_KEY = 'jofi-quiz-state-v1'`
  - [x] Hook `useQuizState` em `src/hooks/useQuizState.ts`
- [x] Componentes quiz (AC: 1, 2, 3, 5, 7)
  - [x] `<Quiz>` orquestrador em `src/components/quiz/Quiz.tsx`
  - [x] `<QuizStep>` renderiza pergunta por tipo (switch case)
  - [x] `<QuizOption>` (template completo em architecture §4.1)
  - [x] `<QuizProgressBar>` com animação ease-out 400ms
  - [x] `<QuizScaleInput>` com slider acessível
  - [x] `<QuizTextInput>` com máscara (usar `use-mask-input` para CEP)
  - [x] `<QuizMultiChoice>` com botão "Próxima" flutuante
  - [x] `<QuizBackButton>` chama dispatch({ type: 'BACK' })
- [x] Roteamento dinâmico (AC: 1, 6)
  - [x] `src/app/quiz/[step]/page.tsx` lê `params.step` e renderiza QuizStep correspondente
  - [x] Redirect para `/obrigado-sem-pet` quando answer `pet-ativo=nao`
  - [x] `src/app/obrigado-sem-pet/page.tsx` (template em front-end-spec §4.1 Screen 7)
- [x] Animações (AC: 7)
  - [x] Dynamic import de framer-motion em QuizStep
  - [x] Fade + slide 280ms com ease-out (ver front-end-spec §9.2)
  - [x] Respeitar `prefers-reduced-motion` (CSS em tokens.css)

## Dev Notes

**Source artifacts:**
- Architecture §4.1 (QuizOption template), §5 (State management), §11.1 (quiz.json schema)
- Front-end Spec §3.1–3.3 (user flows), §4.1 Screens 2–2c (wireframes), §5.1 (componentes), §9 (animations)
- PRD FR2–FR6

**Dependency:** Story 1.1 deve estar merged (projeto bootstrapped).

**Critical rules:**
- Componentes são `'use client'` — interativos (archi §16 rule 9)
- Config JSON é read-only em runtime — deep clone se derivar (rule 13)
- Nunca usar `window` sem guard SSR (rule 7)

### Testing

Referência: architecture §9.1. Obrigatório:
- `src/components/quiz/QuizOption.test.tsx` (exemplo completo em archi §9.1)
- `src/context/QuizContext.test.tsx` — testa START, ANSWER, NEXT, BACK, RESET
- `src/lib/quiz/loader.test.ts` — testa parsing JSON + fallback se inválido
- Coverage mínimo 95% em `src/lib/quiz/`, 80% em `src/components/quiz/`

## File List

**Criados nesta passada:**
- `src/lib/quiz/schema.ts` (Zod discriminated unions)
- `src/lib/quiz/loader.test.ts` (14 testes — getters + schema rejection)
- `src/context/QuizContext.test.tsx` (11 testes — reducer + hydration)

**Modificados nesta passada:**
- `src/lib/quiz/loader.ts` (Zod validation via parseOrThrow)
- `vitest.config.ts` (cast plugin react para resolver vite v5/v7 duplicado)
- `src/lib/tracking/ga4.ts` (dataLayer type fix — Record<string,unknown>[])
- `.eslintrc.json` (registrar plugin @typescript-eslint)
- `package.json` (adicionar devDep @typescript-eslint/eslint-plugin@7)

**Já existentes (implementação prévia, validados):**
- `config/quiz.json`
- `src/lib/quiz/types.ts`, `src/lib/quiz/scoring.ts`
- `src/context/QuizContext.tsx`
- `src/hooks/useQuizState.ts`
- `src/components/quiz/Quiz.tsx`, `QuizStep.tsx`, `QuizOption.tsx`, `QuizMultiChoice.tsx`, `QuizScaleInput.tsx`, `QuizTextInput.tsx`, `QuizProgressBar.tsx`, `QuizBackButton.tsx`
- `src/app/quiz/[step]/page.tsx`
- `src/app/obrigado-sem-pet/page.tsx`

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-04-17 | 0.1 | Initial draft sharded from PRD | @po (aiox-master) |
| 2026-05-12 | 0.2 | Zod validation + 25 tests + status → Ready for Review | @aiox-master (Orion) |
