# Story 4.1: Funil de abandono — eventos por pergunta

## Status

Draft

## Story

**As a** Pedro (decisor),
**I want** ver no GA4 e Meta o funil pergunta-por-pergunta,
**so that** identifico qual pergunta está matando a conclusão e itero copy/formato.

## Acceptance Criteria

1. Cada resposta dada dispara evento `quiz_answer` com parâmetros `{ question_id, answer_value, step_number, variant }`
2. Abandono (inatividade >60s sem avanço OU fechamento de tab) dispara `quiz_abandon` com última pergunta + step
3. Conclusão de quiz (antes da captura) dispara `quiz_complete` com tier calculado
4. Submit bem-sucedido dispara `Lead` + `CompleteRegistration` no Meta; `generate_lead` no GA4
5. Custom definitions configuradas no GA4 para os parâmetros de `quiz_answer`
6. Documento `docs/tracking-plan.md` mapeia todos os eventos, parâmetros, e destinos (Meta + GA4)

## Tasks / Subtasks

- [ ] Expandir event taxonomy (AC: 1, 2, 3, 4)
  - [ ] `src/lib/tracking/events.ts` adiciona:
    - `trackQuizAnswer({ question_id, answer_value, step_number, variant })`
    - `trackQuizAbandon({ last_question_id, step_number, time_spent_sec })`
    - `trackQuizComplete({ tier, score, duration_sec })`
    - `trackGenerateLead({ tier, has_email })`
- [ ] Inatividade detection (AC: 2)
  - [ ] Hook `src/hooks/useInactivityTimer.ts` que dispara callback após 60s sem interaction
  - [ ] Escuta `visibilitychange` (tab hidden) também dispara abandon
  - [ ] QuizStep usa esse hook e chama `trackQuizAbandon` antes de unload
- [ ] Beacon para unload (AC: 2)
  - [ ] Usar `navigator.sendBeacon` em vez de fetch no caso de unload (garantia de envio)
  - [ ] Wrapper `src/lib/tracking/beacon.ts` para eventos críticos
- [ ] Integrar eventos (AC: 1, 3, 4)
  - [ ] QuizContext reducer `ANSWER` dispara `trackQuizAnswer`
  - [ ] QuizContext `FINISH` dispara `trackQuizComplete`
  - [ ] `/api/leads` response success → client dispara `trackGenerateLead` + `fbq('track', 'Lead')` + `fbq('track', 'CompleteRegistration')`
- [ ] GA4 custom definitions (AC: 5)
  - [ ] Doc em `docs/tracking-plan.md` instruindo Pedro a configurar event-scoped custom dimensions no GA4 admin: `tier`, `variant`, `question_id`, `step_number`, `time_spent_sec`
  - [ ] Screenshot print do setup pós-aplicação
- [ ] Tracking plan doc (AC: 6)
  - [ ] `docs/tracking-plan.md` com tabela completa:
    - Column: event_name, trigger, params, Meta mapping, GA4 mapping
    - 15+ eventos documentados (PageView, ViewContent, Lead, CompleteRegistration, InitiateCheckout, quiz_start, quiz_answer, quiz_abandon, quiz_complete, generate_lead, whatsapp_click, sereninho_click, article_click, share)

## Dev Notes

**Source artifacts:**
- Architecture §13 (tracking plan resumo — **expandir para completo**), §16 rule 10 (no console.log)
- PRD FR17, FR18

**Dependency:** Stories 1.4 (base tracking), 2.2 (submit), 3.1 (result), 3.2–3.4 (CTAs).

**Critical rules:**
- Beacon obrigatório para abandon/unload — fetch normal não chega
- Funções de tracking silenciosas (try/catch) — nunca quebram UX

**Tracking plan tabela referência:**
```md
| event_name | trigger | params | Meta | GA4 |
|---|---|---|---|---|
| quiz_start | CTA hero click | variant, utm_source | — | quiz_start |
| quiz_answer | reducer ANSWER | question_id, answer_value, step_number | — | quiz_answer |
| quiz_abandon | 60s inatividade ou tab close | last_question_id, step_number, time_spent_sec | — | quiz_abandon |
| quiz_complete | reducer FINISH | tier, score, duration_sec | — | quiz_complete |
| Lead | /api/leads 200 | value=0, currency=BRL | Lead | generate_lead |
| CompleteRegistration | idem | registration_method=quiz | CompleteRegistration | — |
| InitiateCheckout | result render quente/morno | value, currency, content_name | InitiateCheckout | — |
| whatsapp_click | Story 3.2 | tier, utm_source | Lead (dedupe ok) | whatsapp_click |
| sereninho_click | Story 3.3 | utm_source | InitiateCheckout (dedupe ok) | sereninho_click |
| article_click | Story 3.4 | article_id, utm_content | — | article_click |
| share | Story 3.4 (obrigado-sem-pet) | — | — | share |
```

### Testing

- Unit: cada função de tracking dispara window.fbq + window.gtag com params corretos
- Integration: full flow do quiz → abandon simulado em middle → evento beacon enviado (mock navigator.sendBeacon)
- Manual: GA4 Debug View confirma event-scoped dimensions populadas com valores reais

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-04-17 | 0.1 | Initial draft sharded from PRD | @po (aiox-master) |
