# Story 4.3: Política de privacidade LGPD + opt-out

## Status

Draft

## Story

**As a** Jofi,
**I want** conformidade LGPD documentada e funcional,
**so that** não geramos passivo legal nem comprometemos reputação.

## Acceptance Criteria

1. Rota `/privacidade` publica política escrita com cláusulas LGPD (dados coletados, base legal, retenção, direitos do titular, contato DPO)
2. Texto da política revisado por Pedro (placeholder inicial com TODO de revisão jurídica)
3. Endpoint `/api/opt-out?email={email}&token={hmac}` remove lead do RD Station e adiciona tag `opt-out`
4. Email enviado ao tutor pós-captura (fire and forget via RD Station) inclui link de opt-out assinado (HMAC)
5. Cookie banner mínimo (apenas para cookies de tracking) com aceitar/recusar — recusar bloqueia Meta Pixel e GA4
6. Link "Política de privacidade" visível no footer de toda a LP e na tela de captura

## Tasks / Subtasks

- [ ] Página de privacidade (AC: 1, 2)
  - [ ] `src/app/privacidade/page.tsx` Server Component com MDX ou markdown compilado
  - [ ] Conteúdo placeholder com cláusulas LGPD: dados coletados, base legal (consentimento), retenção, direitos, contato DPO (email)
  - [ ] TODO visível: "Texto revisado pelo jurídico em DD/MM/AAAA"
  - [ ] Layout padrão desktop-friendly (longform)
- [ ] HMAC sign/verify (AC: 3, 4)
  - [ ] `src/lib/hmac/sign.ts` com `sign(email)` → base64url(HMAC-SHA256 com `HMAC_SECRET`) e `verify(email, token)`
  - [ ] Tempo constante na verificação (crypto.timingSafeEqual)
- [ ] Endpoint opt-out (AC: 3)
  - [ ] `src/app/api/opt-out/route.ts` GET: valida HMAC, chama RD Station para remover o contato (ou adicionar tag `opt-out`)
  - [ ] Grava em KV `opt-out:{sha256(email)}` permanente
  - [ ] Retorna página HTML "Você foi desinscrito(a). 💛"
  - [ ] Se HMAC inválido → 404 "link expirado ou inválido"
- [ ] Email pós-captura (AC: 4)
  - [ ] Atualizar `sendConversion` da Story 2.3 para disparar automação RD que envia email
  - [ ] OU enviar email dedicado via SMTP/Resend/SendGrid (decisão com Pedro — provavelmente RD faz isso nativo)
  - [ ] Template do email inclui variável `{opt_out_url}` = `NEXT_PUBLIC_SITE_URL/api/opt-out?email={email}&token={hmac}`
- [ ] Cookie banner (AC: 5)
  - [ ] `src/components/shared/CookieBanner.tsx` — aparece se não há `consent-cookies` em localStorage
  - [ ] Dois CTAs: "Aceitar" (grava `accepted`, habilita scripts) / "Recusar" (grava `rejected`, bloqueia scripts)
  - [ ] Scripts Meta Pixel + GA4 no `layout.tsx` envoltos em check de consent
  - [ ] Link "Política de privacidade" dentro do banner
- [ ] Footer global (AC: 6)
  - [ ] `src/components/shared/Footer.tsx` com links: Política de privacidade, Contato, Logo Jofi
  - [ ] Renderizado no layout raiz (aparece em todas rotas exceto admin)

## Dev Notes

**Source artifacts:**
- Architecture §14.2 (LGPD), §16 rule 10 (no console.log com PII), §10 (env vars HMAC_SECRET)
- PRD FR19 (turnstile), NFR10 (LGPD)
- Front-end Spec §4.1 Screen 8 (privacidade)

**Dependency:** Stories 2.3 (RD client), 1.4 (tracking — agora condicional por consent).

**Critical rules:**
- PII **nunca** logada em JSON logs — sanitizer wrapper em `console.error`
- HMAC_SECRET ≥32 bytes hex, rotacionado anualmente
- Cookie banner deve ser dismissível sem aceitar (não dark pattern)

**Revisão jurídica:** Texto inicial é placeholder baseado em padrões LGPD comuns. Pedro deve obter revisão jurídica real antes de ship produção (marcar como blocker para Epic 4 merge → prod, não para preview).

**Detalhe opt-out:** RD Station tem endpoint `DELETE /platform/contacts/{email}` — pode ser chamado. Alternativa: adicionar tag `opt-out` + campo `opt_out_date` (mais comum). Confirmar preferência com Pedro.

### Testing

- Unit: `sign.test.ts` (HMAC roundtrip), `CookieBanner.test.tsx`
- Integration: `/api/opt-out` valid token → RD mock called com remove; invalid → 404
- Manual: Full flow (captura → recebe email → clica opt-out → confirma unsub)
- A11y: banner keyboard navigable, focus trap se modal

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-04-17 | 0.1 | Initial draft sharded from PRD | @po (aiox-master) |
