# Story 3.4: Roteamento frio — conteúdo educativo + newsletter

## Status

Draft

## Story

**As a** tutor classificado como frio,
**I want** receber conteúdo útil mesmo não sendo prioritário agora,
**so that** a Jofi constrói relacionamento comigo para quando eu precisar no futuro.

## Acceptance Criteria

1. Tela de resultado frio exibe 3 artigos/posts educativos (título + link + thumb) configurados em `config/content.json`
2. CTA secundário "Receber o guia da Jofi por email" adiciona tag `newsletter-jofi` ao lead no RD Station (novo conversion_identifier `jofipet-newsletter`)
3. Se tutor respondeu "sem pet", rota `/obrigado-sem-pet` mostra conteúdo genérico + CTA para compartilhar com amigo que tenha pet
4. Todos os links externos abrem em nova aba com `rel="noopener"`
5. UTMs preservadas nos links de conteúdo para atribuição correta

## Tasks / Subtasks

- [ ] Content config (AC: 1)
  - [ ] `config/content.json` seção `articles` (archi §11.4) com 3 artigos
  - [ ] Loader já existe (Story 2.1)
- [ ] Componente artigos (AC: 1, 4, 5)
  - [ ] `src/components/result/ArticleCards.tsx` renderiza lista de cards
  - [ ] Cada card: emoji + título + link + thumbnail opcional
  - [ ] `target="_blank" rel="noopener"` em todos
  - [ ] URLs construídas com UTMs + `utm_content=quiz-frio-art-{id}`
- [ ] Newsletter CTA (AC: 2)
  - [ ] Reusa `NewsletterCta` da Story 3.3
  - [ ] Mesmo endpoint `/api/leads/newsletter`
- [ ] Tela sem pet (AC: 3)
  - [ ] `src/app/obrigado-sem-pet/page.tsx` — template front-end-spec §4.1 Screen 7
  - [ ] CTA "Compartilhar com um amigo" usa `navigator.share` API quando disponível; fallback para `wa.me?text=...`
  - [ ] URL de compartilhamento: `NEXT_PUBLIC_SITE_URL` + `?ref=amigo&utm_source=indicacao`
- [ ] Tracking
  - [ ] `gtag('event', 'article_click', { article_id, utm_content })` em cada click de artigo
  - [ ] Evento `share` quando usar `navigator.share`

## Dev Notes

**Source artifacts:**
- Architecture §11.4 (content.json articles)
- Front-end Spec §4.1 Screens 6 e 7, §3.2 (flow sem pet)
- PRD FR15 (frio → educativo), FR16

**Dependency:** Stories 3.1 (result base), 3.3 (Newsletter endpoint compartilhado).

**Critical rules:**
- Links externos **sempre** `rel="noopener"` (prevent reverse tabnabbing)
- UTMs propagadas (rule de rastreio)

**Artigos placeholder:** `config/content.json` já tem 3 artigos placeholders (archi §11.4). URLs reais devem ser confirmadas com blog Jofi — se blog não existir, usar placeholders `/blog/placeholder-N` em staging.

**navigator.share:** Feature detect primeiro; fallback para WhatsApp share URL. Em iOS Safari funciona; Android varia.

### Testing

- Unit: `ArticleCards.test.tsx` + `ShareButton.test.tsx`
- Manual: testar share API em iOS e Android real
- Verificar UTMs no GA4 Realtime após click de artigo

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-04-17 | 0.1 | Initial draft sharded from PRD | @po (aiox-master) |
