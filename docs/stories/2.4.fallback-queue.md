# Story 2.4: Fallback queue + retry worker

## Status

Draft

## Story

**As a** Pedro (decisor),
**I want** garantia de que nenhum lead é perdido mesmo se o RD Station ficar fora,
**so that** o investimento em tráfego pago nunca é desperdiçado por instabilidade de terceiros.

## Acceptance Criteria

1. Se chamada ao RD Station falhar com 5xx ou timeout (5s), lead é salvo em Vercel KV com chave `queue:lead:{leadId}` e TTL de 7 dias
2. Response ao client continua retornando `{ success: true, queued: true, correlationId }` — usuário não percebe falha
3. Cron serverless `/api/cron/retry-leads` executa a cada 5 minutos (Vercel Cron)
4. Worker itera todos os leads em queue, tenta re-submit com backoff exponencial (máx 6 tentativas por lead)
5. Sucesso → remove da queue; 6 falhas → move para `queue:dead:{leadId}` + alerta via Sentry
6. Endpoint `/api/admin/queue-stats` (protegido por senha env) retorna contadores: active, dead, last24h_processed
7. Teste de integração simula falha RD Station → confirma entrada em queue → simula worker bem-sucedido → confirma submit

## Tasks / Subtasks

- [ ] Queue library (AC: 1, 4, 5)
  - [ ] `src/lib/kv/queue.ts` com funções: `enqueueLead`, `drainQueue({ batchSize })`, `tickRetry(lead)`, `moveToDead(leadId)`
  - [ ] Chave schema: `queue:active:{leadId}` TTL 7d, `queue:dead:{leadId}` TTL 30d (archi §12.1)
  - [ ] Cada lead em queue guarda: `{ leadId, payload, correlationId, attempts, nextRetryAt }`
  - [ ] Backoff: delay `2^attempts * 60` segundos
- [ ] Integração com `/api/leads` (AC: 1, 2)
  - [ ] Story 2.3 já tem try/catch que chama `enqueueLead` em falha 5xx — confirmar implementação
  - [ ] Response continua `success: true, queued: true`
- [ ] Cron worker (AC: 3, 4, 5)
  - [ ] `src/app/api/cron/retry-leads/route.ts` — template em architecture §12.2
  - [ ] `vercel.json` com cron `*/5 * * * *`
  - [ ] Validação de `CRON_SECRET` no header Authorization
  - [ ] Lock em KV (`rate:cron:lock` TTL 240s) para evitar overlap entre invocações
  - [ ] Processa batch de 50 leads por execução
  - [ ] Move para dead após 6 tentativas
  - [ ] Dispara Sentry alert em cada lead morto
- [ ] Admin stats endpoint (AC: 6)
  - [ ] `src/app/api/admin/queue-stats/route.ts` protegido por basic auth (middleware)
  - [ ] Conta chaves com prefixo `queue:active:*` e `queue:dead:*`
  - [ ] Lê counter `queue:stats:processed:24h` (TTL 24h, INCR no worker)
- [ ] Sentry setup (AC: 5)
  - [ ] Instalar `@sentry/nextjs`
  - [ ] DSN via env var `SENTRY_DSN`
  - [ ] `Sentry.captureMessage('queue_dead_letter', { level: 'warning', extra: { leadId } })` no move-to-dead
- [ ] Integration test (AC: 7)
  - [ ] Test com MSW: POST lead → RD retorna 500 → verificar entrada em queue (KV emulator)
  - [ ] Invocar worker manualmente → MSW retorna 200 → verificar queue vazia
  - [ ] Test backoff: attempt 3 → nextRetryAt está ~8min à frente

## Dev Notes

**Source artifacts:**
- Architecture §12 (KV Queue Strategy — **referência primária**), §7.2 (protected routes), §14 (security)
- PRD FR13, NFR12 (7 dias sem perder leads)

**Dependency:** Story 2.3 (API route existe com try/catch pronto para `enqueueLead`).

**Critical rules:**
- Worker cron usa `runtime = 'nodejs'` (não edge) — acesso a crypto (archi §12.2)
- CRON_SECRET obrigatório — sem ele endpoint é 401
- Lock de overlap é essencial — sem isso dois workers podem processar mesmo lead 2x

**KV emulator local:** Usar `ioredis-mock` ou mock manual via MSW para testes unitários. Produção é Vercel KV real (injetadas env vars automaticamente pelo dashboard).

**Observabilidade:** Logs JSON estruturados com `{ event, leadId, attempts, nextRetryAt, error }` em cada iteração.

### Testing

Referência: architecture §9.1, §12. Obrigatório:
- Unit: `queue.test.ts` com KV emulado cobre enqueue/drain/tick/moveDead
- Integration: flow completo POST → queue → cron → RD (MSW)
- Coverage 90% em `src/lib/kv/queue.ts`
- Manual: invocar cron em staging e verificar Vercel logs

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-04-17 | 0.1 | Initial draft sharded from PRD | @po (aiox-master) |
