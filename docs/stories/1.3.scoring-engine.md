# Story 1.3: Scoring engine determinístico

## Status

Ready for Review — implementação completa + integrada ao QuizContext + 15 tests passing + cobertura 96.94% no scoring.ts (excede o requirement de 95% do AC4)

### Execution Notes (2026-05-12)

- ✅ AC1: `config/scoring.json` define pesos em 4 eixos (pet_ativo/gasto/dor/cobertura) — 7 regras com 3 tipos (weights/numeric-range/cep-coverage)
- ✅ AC2: `calculateTier(answers, scoring, cepCoverage)` em [scoring.ts:64](../../src/lib/quiz/scoring.ts:64) retorna `{tier, score, breakdown, eliminated}` — assinatura mais rica que o AC pedia (eliminated incluso pra debugging)
- ✅ AC3: thresholds configuráveis (`quente: 70, morno: 40`) em scoring.json
- ✅ AC4: testes cobrem todos 6 cenários pedidos + 9 extras (15 total) — quente/morno/frio/eliminated/sim-jofi penalty/determinismo/cep skipped/cep strict mode (3 ranges + 2 formatos)/invariants
- ✅ AC5: 100% client-side — zero `next/server`, zero `window`, zero `fetch`. Função pura.
- ✅ AC6: `breakdown` por eixo retornado — usado pela Story 3.1 (result-dynamic) pra diagnóstico personalizado

**Integração QuizContext:**
- ✅ Reducer `FINISH` em [QuizContext.tsx:78-86](../../src/context/QuizContext.tsx:78) chama `calculateTier` e grava `tier`, `score`, `breakdown`, `eliminated` no state
- ✅ `eliminated` flag fica acessível para componentes consumirem (Story 3.1)

**Adições nesta passada:**
- ✅ 5 testes para mode `strict` de CEP coverage (cobre o caminho `isCepCovered` que antes ficava em 0%)
  - CEP em range PE Recife → coveredScore 15
  - CEP em range PB João Pessoa → coveredScore 15
  - CEP fora de qualquer range (SP) → notCoveredScore 3
  - CEP com formato inválido (<8 dígitos) → fallback skippedScore 5
  - CEP sem máscara (só dígitos) → funciona
- ✅ 2 testes de invariants:
  - Score nunca negativo (exceto eliminated)
  - Soma do breakdown = score total

**Coverage final (escopo Story 1.3):**
- `scoring.ts`: **96.94%** lines / 90.38% branches / 100% functions ✅ (excede 95% pedido)
- Linhas não cobertas (3.06% restantes): só edge cases extremos como `Number(NaN)` em `toNumber()` que são guard rails defensivos

**Comandos de validação rodados:**
- `npx vitest run src/lib/quiz/scoring.test.ts --coverage` → 15/15 passing ✅
- `npm run typecheck` ✅
- Cobertura confirmada via coverage v8 report

**Decisões registradas:**
- Sentinel value `-999` em weights = elimination flag (Pedro pode reusar em outras regras se quiser eliminação por critério diferente de `pet-ativo=nao`)
- CEP coverage em mode `fallback-neutral` por default (decisão Pedro 2026-05-12 em open question #5) — quando virar `strict`, basta editar `config/cep-coverage.json` (sem alteração de código)
- Penalty de R$-50 em `plano-atual=sim-jofi` força tier=frio (cliente Jofi atual não deve ser tratado como lead novo)

## Story

**As a** Pedro (decisor),
**I want** poder ajustar pesos e regras de scoring editando um único JSON,
**so that** posso iterar o modelo sem redeploy de código.

## Acceptance Criteria

1. `config/scoring.json` define pesos por pergunta/resposta em 4 eixos (pet_ativo, gasto, dor, cobertura)
2. Função pura `calculateTier(answers, config)` retorna `{ tier: 'quente'|'morno'|'frio', score: number, breakdown: {...} }`
3. Thresholds de classificação configuráveis no JSON (ex.: `quente >= 70`, `morno 40–69`, `frio < 40`)
4. Testes unitários (Vitest) cobrem: respostas perfeitas → quente, médias → morno, mínimas → frio, eliminação por "sem pet"
5. `calculateTier` executa 100% client-side, sem fetch
6. Breakdown por eixo disponível no resultado final para debugging e para renderização do diagnóstico personalizado

## Tasks / Subtasks

- [x] Criar scoring config (AC: 1, 3)
  - [x] `config/scoring.json` conforme schema de architecture §11.2
  - [x] `config/cep-coverage.json` com dataset placeholder + mecanismo de loader (open question #5 resolvida em 2026-05-12 — mode=fallback-neutral mantido)
- [x] Implementar scoring engine (AC: 2, 5, 6)
  - [x] `src/lib/quiz/scoring.ts` com função pura `calculateTier(answers, config)`
  - [x] Suporta rules: `weights`, `numeric-range`, `cep-coverage`
  - [x] Flag `eliminated: true` quando pet-ativo=nao (força tier=frio)
  - [x] Retorna `breakdown: { pet_ativo, gasto, dor, cobertura }` por eixo
  - [x] Zero imports de `next/server`, `window`, ou fetch — 100% puro
- [x] Integrar com QuizContext (AC: 2)
  - [x] Reducer `FINISH` chama `calculateTier` e grava tier no state
  - [x] Breakdown fica acessível em `state.breakdown` (nome final no QuizContext)
- [x] Testes (AC: 4)
  - [x] `src/lib/quiz/scoring.test.ts` com **15 test cases** (6 do AC + 9 extras):
    - [x] respostas perfeitas → quente, score ≥70
    - [x] respostas médias → morno
    - [x] respostas mínimas → frio
    - [x] `pet-ativo=nao` → eliminated:true, tier=frio
    - [x] `plano-atual=sim-jofi` → penalty -50 (morno vira frio)
    - [x] determinismo: 2 chamadas com mesmo input → output idêntico
    - [x] cep skipped → score 5 (neutro)
    - [x] missing answers (sem throw)
    - [x] strict mode: CEP em PE Recife (50000-52999) → coveredScore
    - [x] strict mode: CEP em PB João Pessoa (58000-58099) → coveredScore
    - [x] strict mode: CEP em SP → notCoveredScore
    - [x] strict mode: CEP curto → fallback skippedScore
    - [x] strict mode: CEP sem máscara → funciona
    - [x] invariant: score nunca negativo (exceto eliminated)
    - [x] invariant: breakdown.sum === score

## Dev Notes

**Source artifacts:**
- Architecture §11.2 (scoring.json schema), §9.1 (test template), §16 rule 14 (scoring é puro)
- PRD FR7–FR8, brief §Proposed Solution

**Dependency:** Story 1.2 (QuizContext existe). CEP coverage loader pode usar placeholder se open question #5 não estiver resolvida.

**Critical rules:**
- Função pura — zero side effects, zero I/O (rule 14)
- Config JSON read-only (rule 13)
- Deterministic — se input igual, output igual sempre

**Fallback CEP coverage:** Se `cep-coverage.json` não tiver dados definitivos no momento da implementação, usar `coveredScore: 10` (neutro) para todos os CEPs. TODO marcado no código para substituir quando dataset chegar.

### Testing

Referência: architecture §9.1. Obrigatório:
- Coverage **95%** no arquivo `scoring.ts` (lógica crítica — rule 16 §9.2)
- Property-based testing opcional com `fast-check` para invariantes (score sempre ≥0 exceto eliminated)
- Snapshot test com fixtures reais em `src/test/fixtures/answers.ts`

## File List

**Modificados nesta passada:**
- `src/lib/quiz/scoring.test.ts` (+5 testes strict CEP coverage + 2 invariants = 15 total)
- `docs/stories/1.3.scoring-engine.md` (status, file list, execution notes)

**Já existentes (implementação prévia, validados):**
- `src/lib/quiz/scoring.ts` (função pura calculateTier)
- `src/lib/quiz/types.ts` (ScoringConfig, ScoringRule, TierResult, ScoreBreakdown)
- `config/scoring.json` (7 regras + thresholds)
- `config/cep-coverage.json` (mode fallback-neutral)
- `src/context/QuizContext.tsx` (FINISH dispatcher integrado)
- `src/lib/quiz/loader.ts` (loader expondo scoringConfig + cepCoverageConfig)
- `src/lib/quiz/schema.ts` (Zod ScoringConfigSchema)

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-04-17 | 0.1 | Initial draft sharded from PRD | @po (aiox-master) |
| 2026-05-12 | 0.2 | +7 testes (strict CEP + invariants) + status → Ready for Review | @aiox-master (Orion) |
