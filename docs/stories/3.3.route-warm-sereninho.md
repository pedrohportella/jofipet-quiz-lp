# Story 3.3: Roteamento morno — Sereninho R$49,90

## Status

Ready for Review — SereninhoCta + UTM preservation + NewsletterCta secundário; aguarda @qa gate

### Execution Notes (2026-05-12)

- ✅ AC1: `<SereninhoCta>` redireciona para `process.env.NEXT_PUBLIC_SERENINHO_CHECKOUT_URL`
- ✅ AC2: URL via env var (atualmente placeholder `jofi.pet/?...` — substituir quando URL real do checkout estiver disponível, open question #4)
- ✅ AC3: UTMs originais lidas via `loadStoredUtms()` + `utm_content=quiz-morno` adicionado se não presente
- ✅ AC4: `trackInitiateCheckout({ tier: 'morno', value: 49.9 })` + `trackSereninhoClick({ utms })` no clique
- ✅ AC5: Fallback "Checkout em configuração — em breve" se env vazio
- ✅ AC6: Link secundário "Ou receba dicas por email" via `<NewsletterCta>` integrado no ResultWarm (POST `/api/leads/newsletter` com tag `newsletter-jofi`)

**Tech debt:**
- URL do Sereninho ainda é fallback institucional (`jofi.pet/?...`) — quando Pedro tiver URL real do checkout, troca `NEXT_PUBLIC_SERENINHO_CHECKOUT_URL` no painel Vercel + redeploy

## Story

**As a** tutor classificado como morno,
**I want** conhecer o Sereninho como primeira experiência com a Jofi por um ticket acessível,
**so that** posso testar o serviço antes de assinar um plano maior.

## Acceptance Criteria

1. CTA "Conhecer o Sereninho por R$49,90" redireciona para URL de checkout existente da Jofi
2. URL do checkout em env var `NEXT_PUBLIC_SERENINHO_CHECKOUT_URL`
3. UTMs originais + `utm_content=quiz-morno` são adicionadas à URL do checkout
4. Evento `InitiateCheckout` Meta Pixel dispara no clique
5. Fallback se checkout URL não configurada → mostra mensagem "em breve" + opt-in de email
6. Página de resultado morno também exibe link secundário "ou receba dicas por email" que adiciona tag `newsletter-jofi` no RD Station (fire and forget)

## Tasks / Subtasks

- [ ] Resolver open question #4 antes do merge
  - [ ] URL exata do checkout Sereninho confirmada por Pedro
- [ ] Componente CTA (AC: 1, 2, 3)
  - [ ] `src/components/result/SereninhoCta.tsx`
  - [ ] Construir URL: base + query params das UTMs + `utm_content=quiz-morno`
  - [ ] Usar `URLSearchParams` para evitar bugs de encoding manual
- [ ] Fallback gracioso (AC: 5)
  - [ ] Se `process.env.NEXT_PUBLIC_SERENINHO_CHECKOUT_URL` vazio, mostrar copy "Em breve" + ativar `NewsletterCta`
- [ ] Newsletter CTA secundário (AC: 6)
  - [ ] `src/components/result/NewsletterCta.tsx` POST para `/api/leads/newsletter` (reaproveita infra da Story 2.3)
  - [ ] Endpoint usa conversion_identifier `jofipet-newsletter` + tag `newsletter-jofi`
  - [ ] "Fire and forget" — click → loading spinner → success toast, sem aguardar confirmação
- [ ] Tracking (AC: 4)
  - [ ] `trackEvent('InitiateCheckout', { value: 49.90, currency: 'BRL', content_name: 'sereninho' })`
  - [ ] `gtag('event', 'sereninho_click', { utm_source })`

## Dev Notes

**Source artifacts:**
- Architecture §11.3 (results.json morno), §13.1 (Meta events)
- Front-end Spec §4.1 Screen 5
- PRD FR15 (morno → Sereninho)

**Dependency:** Story 3.1 (result base). Para `/api/leads/newsletter`, reusa client RD Station da Story 2.3.

**Critical rules:**
- UTMs devem ser preservadas (rule implied pela PRD FR16)
- Fallback sem checkout é degrade graceful, não erro

**Newsletter endpoint:** Criar `src/app/api/leads/newsletter/route.ts` — reusa `sendConversion` com payload:
```ts
{
  event_type: 'CONVERSION',
  event_family: 'CDP',
  payload: {
    conversion_identifier: 'jofipet-newsletter',
    email,
    tags: ['newsletter-jofi', 'quiz-jofipet'],
  }
}
```
Rate limit separado: `rate:newsletter:{ip}` max 3/hora.

### Testing

- Unit: `SereninhoCta.test.tsx` — URL construction com UTMs, fallback quando env vazio
- Integration MSW: newsletter endpoint funciona com RD mock
- Manual: fluxo completo em preview

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-04-17 | 0.1 | Initial draft sharded from PRD | @po (aiox-master) |
